apiVersion: apps/v1
kind: Deployment
metadata:
  name: noah-backend
  namespace: noah-dev
spec:
  replicas: 2
  selector:
    matchLabels:
      app: noah-backend
  template:
    metadata:
      labels:
        app: noah-backend
      annotations:
        checksum/config: "0447dcfff6f95bfa5a69d8e145ecbdb8178e3d9597d18b6bbc90f3075962a3d6"
        checksum/secret: "e29ee104696acd566985548fe5f9c3c8084f317b7cc4da20ae98e044ace5dec2"
    spec:
      containers:
        - name: backend
          image: noah-backend:dev
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 8000
          envFrom:
            - configMapRef:
                name: noah-backend-config
            - secretRef:
                name: noah-backend-secret
          command: ["sh", "-c"]
          args:
            - >
              python manage.py migrate &&
              python manage.py collectstatic --noinput &&
              gunicorn noah_food.asgi:application -k uvicorn.workers.UvicornWorker --bind 0.0.0.0:8000 --workers 2
          livenessProbe:
            httpGet:
              path: /healthz
              port: 8000
            initialDelaySeconds: 15
            periodSeconds: 10
            timeoutSeconds: 2
            failureThreshold: 3
          readinessProbe:
            httpGet:
              path: /readyz
              port: 8000
            initialDelaySeconds: 10
            periodSeconds: 5
            timeoutSeconds: 2
            failureThreshold: 3
          resources:
            requests:
              cpu: "100m"
              memory: "256Mi"
            limits:
              cpu: "500m"
              memory: "512Mi"
---
apiVersion: v1
kind: Service
metadata:
  name: noah-backend-svc
  namespace: noah-dev
spec:
  selector:
    app: noah-backend
  ports:
    - port: 8000
      targetPort: 8000
