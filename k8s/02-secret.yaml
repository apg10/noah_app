apiVersion: v1
kind: Secret
metadata:
  name: noah-backend-secret
  namespace: noah-dev
type: Opaque
stringData:
  # Plantilla: NO guardar secretos reales en git.
  DJANGO_SECRET_KEY: "REPLACE_WITH_SECURE_VALUE"
  POSTGRES_PASSWORD: "REPLACE_WITH_SECURE_VALUE"
